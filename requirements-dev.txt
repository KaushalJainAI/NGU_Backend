# Development / type-checking / lint tooling — not needed at runtime.
# Install with:  pip install -r requirements-dev.txt

# Type stubs (remove DRF/Django Pylance & mypy false positives)
django-stubs==6.0.5
djangorestframework-stubs==3.17.0

# Lint + format (one tool) and type checker
ruff==0.14.5
mypy==1.18.2

# Git hook runner
pre-commit==4.4.0
